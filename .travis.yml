language: php

php:
  - 7
  - 7.1
  - 7.2
  - 7.3
  - 7.4

install: travis_retry composer install

script: composer ci

notifications:
  email:
    recipients:
      - jeroendedauw@gmail.com
    on_success: change
    on_failure: always

cache:
  directories:
    - $HOME/.composer/cache
